import * as React from "react";
import { JSX } from "react";


const Cart: React.FC = () => {
    const build = (): JSX.Element => {
        
        return (
            <div>

            </div>
        )
    }
    return build();
}

export default Cart;
